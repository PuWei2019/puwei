<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <!--namespace属性：指定当前文件和哪个接口经行映射，指定完整路径文件路径结构 -->
<mapper namespace="com.cy.store.mapper.AddressMapper">
    <resultMap id="AddressEntityMap" type="com.cy.store.entity.Address">
        <!--名称不一致的字段匹配指定 一致可以不写 -->
        <!--result标签经行匹配 column:表字段 property:java实体类 例:表t_user.is_delete java实体类:user.isDelete -->
        <!--主键用id 其他用result 完整版需要写id 表示主键 -->
        <id column="aid" property="aid"/>
        <result column="province_name" property="provinceName"></result>
        <result column="province_code" property="provinceCode"></result>
        <result column="city_name" property="cityName"></result>
        <result column="city_code" property="cityCode"></result>
        <result column="area_name" property="areaName"></result>
        <result column="area_code" property="areaCode"></result>
        <result column="is_default" property="isDefault"></result>
        <result column="created_user" property="createdUser"></result>
        <result column="created_time" property="createdTime"></result>
        <result column="modified_user" property="modifiedUser"></result>
        <result column="modified_time" property="modifiedTime"></result>
    </resultMap>
    <insert id="InsertAddress" useGeneratedKeys="true" keyProperty="aid">
        INSERT INTO t_address (
        uid, name, province_name, province_code, city_name, city_code, area_name, area_code, zip,
        address, phone, tel,tag, is_default, created_user, created_time, modified_user, modified_time
        ) VALUES (
        #{uid}, #{name}, #{provinceName}, #{provinceCode}, #{cityName}, #{cityCode}, #{areaName},
        #{areaCode}, #{zip}, #{address}, #{phone}, #{tel}, #{tag}, #{isDefault}, #{createdUser},
        #{createdTime}, #{modifiedUser}, #{modifiedTime}
        )
    </insert>
    <select id="CountByUid" resultType="java.lang.Integer">
        SELECT count(*)
        FROM t_address
        WHERE uid = #{uid}
    </select>

    <select id="findByUid" resultType="com.cy.store.entity.Address" resultMap="AddressEntityMap">
        SELECT *
        FROM t_address
        WHERE uid = #{uid}
        ORDER BY is_default DESC,created_time DESC
    </select>

    <select id="findByAid" resultType="com.cy.store.entity.Address" resultMap="AddressEntityMap">
        SELECT *
        FROM t_address
        WHERE aid = #{aid}
    </select>

    <update id="updateNonDefault">
        UPDATE t_address
        SET is_default = 0
        WHERE uid = #{uid}
    </update>


    <update id="updateDefaultByAid">
        UPDATE t_address
        SET is_default = 1,
        modified_user = #{modifiedUser},
        modified_time = #{modifiedTime}
        WHERE aid = #{aid}
    </update>

    <delete id="deleteByAid">
        DELETE
        FROM
        t_address
        WHERE aid = #{aid}
    </delete>

    <select id="findLastModified" resultMap="AddressEntityMap" resultType="com.cy.store.entity.Address">
        SELECT *
        FROM t_address
        WHERE uid = #{uid}
        ORDER BY modified_time DESC limit 0,1
    </select>

</mapper>